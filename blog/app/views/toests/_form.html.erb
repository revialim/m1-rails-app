<%= form_for(toest) do |f| %>
  <% if toest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(toest.errors.count, "error") %> prohibited this toest from being saved:</h2>

      <ul>
      <% toest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :crunchiness %>
    <%= f.number_field :crunchiness %>
  </div>

  <div class="field">
    <%= f.label :sweet %>
    <%= f.check_box :sweet %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
