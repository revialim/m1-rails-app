<%= form_for(picture_entry) do |f| %>
  <% if picture_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture_entry.errors.count, "error") %> prohibited this picture_entry from being saved:</h2>

      <ul>
      <% picture_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div class="field">
    <%= f.label :picture %>
    <%= f.text_field :picture %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
